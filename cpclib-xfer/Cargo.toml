[package]

name = "cpclib-xfer"
version = "0.5.0"
authors = ["Romain Giot <giot.romain@gmail.com>"]
edition = "2021"
license = "MIT"
description = "cpclib libraries related to snapshots"
repository = "https://github.com/cpcsdk/rust.cpclib"

[package.metadata.workspaces]
independent = true

[dependencies]
cpclib-disc = "0.5.0"
cpclib-sna = "0.5.0"
cpclib-common = "0.5.0"

arrayref= "0.3.6"
cfg-if = "1.0.0"
custom_error = "1.9.2"
delegate = "0.6.2"
either = "1.6.1"
failure = "0.1.8"
getset = "0.1.1"
matches = "0.1.8"
#memchr = "2.4.0"
serde = { version = "1.0.126", features = ["derive"] }
tempfile= "3.2.0"

path-absolutize = { version = "3.0.10", optional = false }
curl = { version = "0.4.38", optional = false }
reqwest = { version = "0.11.4", features = ["blocking"] }


[build-dependencies]
built = { version = "0.5.1", features = ["chrono"] }

